/**
 * React Adapter for Hexagonal Architecture.
 * Exports only what the app needs from React.
 */
export {
  default as React,
  useState,
  useEffect,
  useRef,
  useCallback,
  useMemo,
  useContext,
  createContext,
  Fragment,
  createElement,
} from "react";